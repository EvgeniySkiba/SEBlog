    @helper Tags(System.Web.Mvc.HtmlHelper htmlHelper,
    IList<SeBlog.Core.Objects.Tag> tags)
{
foreach (var tag in tags)
{
        <div class="tags-div">
            @SeBlog.Web.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
}
}